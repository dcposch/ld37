{
  "name": "ld37",
  "description": "ludum dare 37",
  "version": "0.0.0",
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "bugs": {
    "url": "https://github.com/dcposch/ld37/issues"
  },
  "dependencies": {
    "express": "^4.14.0",
    "gl-mat4": "^1.1.4",
    "gl-vec3": "^1.0.3",
    "gl-vec4": "^1.0.1",
    "regl": "^1.2.1"
  },
  "devDependencies": {
    "brfs": "^1.4.3",
    "browserify": "^13.1.0",
    "mkdirp": "^0.5.1",
    "nodemon": "^1.11.0",
    "standard": "^8.3.0",
    "watchify": "^3.7.0"
  },
  "license": "UNLICENSED",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/dcposch/ld37.git"
  },
  "scripts": {
    "build": "mkdirp build && browserify --debug src/client > build/bundle.js",
    "start": "npm run build && node ./src/server",
    "test": "standard",
    "watch": "npm run watch-js & npm run watch-server",
    "watch-js": "mkdirp build && watchify --debug src/client -o build/bundle.js",
    "watch-server": "nodemon ./src/server -e js,pug,css -i static/"
  },
  "standard": {
    "ignore": [
      "build/"
    ]
  }
}
